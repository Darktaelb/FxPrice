# Directory where placed FxPrice project

# Before the run you need to:

1. "npm install" in both "client" and "server" directories.

# To start locally you need to:

1. "npm start" in both "client" and "server" directories.
